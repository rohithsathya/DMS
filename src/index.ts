//"files": ["lib/**/*"] - for package.json


//workflow to publish
//1. >npm run build
//2. >npm publish

//import {VendorManagementService} from './vms/vms';

export const Greeter = (name: string) => `Hello ${name}`;